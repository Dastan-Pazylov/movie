$('.movie').slick({
    slidesToShow: 3,
    dots:true,
    centerMode: true,
    centerPadding: '60px',
})

$('.new').slick({
    slidesToShow: 4,
    dots: true,
})

$('.video').slick({
    slidesToShow:2,
    dots:true,

})

$('.casts').slick({
    slidesToShow: 4,

})